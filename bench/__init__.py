"""insitubatch benchmark suite (dev tooling; not shipped in the wheel).

Run the whole suite with one command:

    uv run python -m bench            # quick local smoke grid
    uv run python -m bench --help     # options (engines, chunk sizes, S3, ...)
"""
